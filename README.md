# Timesheets
A fully automated **employee time-tracking system** built using **Google Apps Script** and **Google Sheets**.   This script allows employees to clock in, clock out, and log their work sessions with total hours calculated automatically.   Each session and related tasks are saved into a structured Google Sheet with audit logging.
